rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 本人（ログイン中のUIDと一致するドキュメント）のみ読み書き可能。
    match /progress/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
